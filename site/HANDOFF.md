# Передача работы по AK-LOEWEN

## Последнее обновление: Vercel опубликован, Upstash ожидает владельца

Пользователь вновь разрешил подключить Upstash/Telegram и обновить Vercel. Новый публичный preview приложения из `62e8e77`:
https://ak-loewen-release-r7mo6szax-zumeeeeer-6684s-projects.vercel.app/ru/

Deployment `dpl_EsNupjBJ79utEu2TcAK6YyuHNafw`: READY, target=null (preview). Проверено HTTP 200, переключение темы и закрытое расписание на RU/390px. В page-data live=false; POST пустого тела в /api/trial-requests возвращает 503/not_configured, отправки сообщений не было.

Upstash не создан: `vercel integration list --all` вернул No resources found. Повтор установки `upstash/upstash-kv` с free/fra1, eviction=false, prodPack=false, autoUpgrade=false, только preview, остановлен Vercel с integration_terms_acceptance_required. Пользователю открыта страница:
https://vercel.com/zumeeeeer-6684s-projects/~/integrations/accept-terms/upstash?source=cli

Ждать подтверждения принятия условий владельцем, затем повторить установку с теми же параметрами. Не утверждать, что приём заявок работает. TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID_AK присутствуют как Hidden Preview secrets; FORM_DELIVERY_ENABLED пока отсутствует. Не печатать секреты.

После установки: проверить Redis/Lua и повторные запросы; устранить риски зависшего pending и ошибки сохранения после Telegram acknowledgment; включить доставку только после проверки, обновить preview и выполнить одну явно тестовую заявку с проверкой Redis+Telegram acknowledgment. Дополнительная юридическая информация по-прежнему не подтверждена. Ниже — история предыдущей передачи.

## Где остановились

Ветка `codex/release-a`, приложение в `site/`. Последний предыдущий коммит — `e760baa` (standalone-ассеты и передача работы), серверный — `3f6ee9a`. В продолжении от 2026-09-13 пользователь запросил исправление навигации, скрытое расписание, дополнительные анимации, Telegram-кнопку, светлую/тёмную тему и push. Юридические данные и подключение Upstash он явно отложил — не запрашивать их повторно без необходимости и не активировать доставку молча.

Последний проверенный публичный preview содержит коммит `44de4dd`:
https://ak-loewen-release-kepytq765-zumeeeeer-6684s-projects.vercel.app/de/

Подготовленный hosted endpoint в этот preview ещё не развёрнут. Реальная заявка из браузера в Telegram не проверена. Тестовое сообщение отправляли напрямую Bot API; пользователь подтвердил получение. Не путать это с проверкой всей формы.

## Согласованные ограничения

- Сохранить основные тексты, спортивный стиль, существующие перчатки и ScrollCraft. Пользователь отдельно одобрил добавление светлой темы и текущие доработки анимации; это не разрешение на несвязанные изменения Release B.
- Цвета: #E85A22, #C4501E, #FF7A3D. Четыре языка DE/RU/UK/TR.
- ТЗ v5 DOCX подробнее, чем MD. Оба лежат в документационной ветке `Ak-loewen`, `docs/TZ-AK-LOEWEN-gGmbH-v5-source-of-truth.*`. Не переключать рабочую ветку поверх изменений; читать через git show.
- Production и Release B не одобрены. Делать preview, коммитить и пушить значимые изменения.
- Namig Aliyev: предоставленный пользователем портрет уже включён в AVIF/WebP/JPEG. Биографию и достижения не выдумывать. Anar пока со старой явно обозначенной заглушкой.
- Werwolf 8 — подтверждённое место тренировок. Юридический адрес не подтверждён; правовые страницы пока заглушки.

## Что работает

Фильтры расписания, выбор группы из расписания, форма с одним из трёх контактов, временем и комментарием; сохранение полей при смене языка; автоматический язык на корневом адресе; диалог Telegram. Белое подчёркивание активного языка убрано, фон и оранжевый индикатор сохранены.

Бот @ak_loewenbot, получатель @kosstyukov. TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID_AK сохранены как скрытые Preview secrets Vercel. Не копировать значения из истории, не печатать их, не коммитить. Username получателя не заменяет числовой chat ID. Не отправлять дополнительные тестовые сообщения без необходимости.

## Незавершённый backend

`api/trial-requests.js`, `server/hosted-trial.js`: заготовка Vercel endpoint с Redis, атомарным claim, лимитом 12 запросов/5 минут и журналом на 30 дней. Журнал содержит fingerprint, статус и технические идентификаторы, без текста заявки. Есть тесты с in-memory ledger; настоящий Redis и Lua ещё не проверены.

Upstash НЕ создан: Vercel требует принятия Marketplace terms владельцем. Пользователь попросил отложить. План при возобновлении: free, fra1, eviction=false, autoUpgrade=false, только Preview. FORM_DELIVERY_ENABLED остаётся выключенным. Не выдавать текущую заготовку за законченный backend.

Перед активацией проверить: реальное исполнение Lua и конкурентные запросы; восстановление зависшего pending после сбоя; поведение при сбое сохранения после подтверждения Telegram; лимиты тела для standalone Vercel runtime; бюджет timeout; авторизация/CSRF; правила хранения и удаления. TTL 30 дней сейчас ограничивает срок дедупликации. Webhook, Owner/Trainer/Viewer и bootstrap из DOCX пока не реализованы.

## Исправления в текущем продолжении

Скачиваемый HTML теперь включает все варианты портрета Намига. Его встроенные страницы всегда demo, даже если hosted build live. Endpoint явно проверяет FORM_DELIVERY_ENABLED. Проверка: `node tests/build-review.mjs` (в конце возвращает сборку в demo).

Дополнительно: устранено двойное смещение якорей (scroll-padding + scroll-margin), из-за которого пункт расписания оставался на тренерах; расписание закрыто в details по умолчанию; ссылка занятия переносит его время в форму. Добавлены плавающая Telegram-кнопка с ником бота и поясняющим диалогом, сохраняемый переключатель темы, контрастные светлые стили и появления секций/VALSET с поддержкой reduced-motion. Основные тексты и подтверждённые данные не переписаны.

Пользователь попросил применить пакет `C:/Users/Vitalii/Desktop/paragon-swarm-main/`, затем явно согласовал адаптацию Python-процесса Paragon к браузерному JavaScript. Независимый Test Designer подготовил проверки без чтения исходников; Solver завершил исходные файлы без доступа к скрытым тестам; Test Auditor проверил требования и черновик. Родитель запускал итоговый набор как детерминированный Runner. Скрытые тесты удалены и не закоммичены.

Локально выполнены lint, 18 unit-тестов, build, проверки form-browser/outcomes и release-a (4 языка × 5 ширин, фильтры, раскрытие, диалог, legal routes); отчёт `test-results/release-a/report.json` от 2026-09-13 содержит issues: []. Визуально просмотрены светлая/тёмная темы на 390/1440 px и VALSET. Прямые Telegram-сообщения не отправлялись.

## Остановка по просьбе пользователя — минимальная версия

Пользователь сообщил об остатке лимитов и попросил готовые файлы вместо дальнейших доработок. Финальная сборка и lint проходят. Мобильному меню добавлена оранжевая отметка и сохранение scrollX/Y при открытии/закрытии; блокируется overflow корневого элемента вместо body, фокус не прокручивает страницу. Текст Telegram-диалога честно предупреждает, что обработка сообщений для записи пока не подключена.

Последний полный Paragon Runner: 50 passed / 8 failed. Ранее исправлена ошибка самого теста: Range.getClientRects учитывал невидимый текст закрытого details. Оставшиеся 8 результатов относятся к мобильному меню: Playwright locator.click() на sticky-кнопке дополнительно прокручивает страницу. После последней правки проверка координатным mouse.click по видимой кнопке подтвердила: до и после повторного открытия scrollY=2939, schedule.top≈85.95, активен #stundenplan. Полный независимый набор после этого НЕ доведён до VERIFIED; не заявлять обратное. При продолжении проверить сценарий настоящих pointer/keyboard действий и исправить автопрокрутку тестового инструмента, затем повторить независимый аудит. Не расширять порог scrollspy и не подменять активный раздел hash-ссылкой.

Новый Vercel preview в этом минимальном завершении не публиковался. Старая ссылка выше показывает прежнюю версию. Новые файлы передаются локально: standalone HTML со встроенными ресурсами и архив отслеживаемых исходников site/. Секреты и node_modules в архив не включать.

После возобновления: (1) завершить браузерный аудит мобильного меню; (2) обновить Vercel preview из codex/release-a; (3) когда пользователь вернётся к отложенным пунктам — Upstash, юридические тексты и полноценная обработка заявок/Telegram webhook. Дальнейшая визуальная полировка — только после просмотра пользователем минимальной версии.

## Проверки и команды

Node >=24, обычный ESM/vanilla JS, без React/Next. `npm run lint`, `npm test`, `npm run build`. Перед продолжением было 18 успешных unit-тестов. Браузерные тесты: `tests/form-browser.mjs`, `tests/portrait-browser.mjs`, `tests/release-a.mjs`, `tests/outcomes.mjs`; URL через TEST_BASE_URL. Они по умолчанию используют Chrome на Windows. Демонстрационные тесты не должны отправлять реальные заявки.

Текущий проверочный сервер запущен на http://127.0.0.1:4187/de/ . После паузы проверить доступность: процессы могут остановиться. Не убивать чужой процесс на 4173.

Vercel project: ak-loewen-release-a, scope zumeeeeer-6684s-projects, projectId prj_0kG9RBjUgIn4UktNF1qYU0cCgRvU. Root directory site, build npm run build, output dist. SSO preview отключён. Автодеплой Git выключен в vercel.json. Ранее CLI ошибочно выбрал production для первого deploy, поэтому preview создавали API /v13/deployments с target ОПУЩЕННЫМ, gitSource ref codex/release-a. Проверять ответ target=null и READY. Не печатать полную конфигурацию Vercel: там могут быть bypass secrets.

## Следующее действие

Сверить git status и этот файл. Продолжать независимые доработки Release A по DOCX, сохраняя отложенные пункты. После разрешения вернуться к Upstash — закончить backend, затем отдельно проверить полный путь браузер → API → Redis → Telegram. Перед каждым отчётом различать: код сохранён, локально проверен, развёрнут, реально доставляет.
