# Передача работы по AK-LOEWEN

## Где остановились

Ветка `codex/release-a`, приложение в `site/`. Перед этой передачей последний серверный коммит — `3f6ee9a`. Пользователь попросил сохранить всё на GitHub, затем продолжить. Юридические данные и подключение Upstash он явно отложил — не запрашивать их повторно без необходимости и не активировать доставку молча.

Последний проверенный публичный preview содержит коммит `44de4dd`:
https://ak-loewen-release-kepytq765-zumeeeeer-6684s-projects.vercel.app/de/

Подготовленный hosted endpoint в этот preview ещё не развёрнут. Реальная заявка из браузера в Telegram не проверена. Тестовое сообщение отправляли напрямую Bot API; пользователь подтвердил получение. Не путать это с проверкой всей формы.

## Согласованные ограничения

- Сохранить основные тексты, тёмный спортивный стиль, существующие перчатки и ScrollCraft.
- Цвета: #E85A22, #C4501E, #FF7A3D. Четыре языка DE/RU/UK/TR.
- ТЗ v5 DOCX подробнее, чем MD. Оба лежат в документационной ветке `Ak-loewen`, `docs/TZ-AK-LOEWEN-gGmbH-v5-source-of-truth.*`. Не переключать рабочую ветку поверх изменений; читать через git show.
- Production и Release B не одобрены. Делать preview, коммитить и пушить значимые изменения.
- Namig Aliyev: предоставленный пользователем портрет уже включён в AVIF/WebP/JPEG. Биографию и достижения не выдумывать. Anar пока со старой явно обозначенной заглушкой.
- Werwolf 8 — подтверждённое место тренировок. Юридический адрес не подтверждён; правовые страницы пока заглушки.

## Что работает

Фильтры расписания, выбор группы из расписания, форма с одним из трёх контактов, временем и комментарием; сохранение полей при смене языка; автоматический язык на корневом адресе; диалог Telegram. Белое подчёркивание активного языка убрано, фон и оранжевый индикатор сохранены.

Бот @ak_loewenbot, получатель @kosstyukov. TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID_AK сохранены как скрытые Preview secrets Vercel. Не копировать значения из истории, не печатать их, не коммитить. Username получателя не заменяет числовой chat ID. Не отправлять дополнительные тестовые сообщения без необходимости.

## Незавершённый backend

`api/trial-requests.js`, `server/hosted-trial.js`: заготовка Vercel endpoint с Redis, атомарным claim, лимитом 12 запросов/5 минут и журналом на 30 дней. Журнал содержит fingerprint, статус и технические идентификаторы, без текста заявки. Есть тесты с in-memory ledger; настоящий Redis и Lua ещё не проверены.

Upstash НЕ создан: Vercel требует принятия Marketplace terms владельцем. Пользователь попросил отложить. План при возобновлении: free, fra1, eviction=false, autoUpgrade=false, только Preview. FORM_DELIVERY_ENABLED остаётся выключенным. Не выдавать текущую заготовку за законченный backend.

Перед активацией проверить: реальное исполнение Lua и конкурентные запросы; восстановление зависшего pending после сбоя; поведение при сбое сохранения после подтверждения Telegram; лимиты тела для standalone Vercel runtime; бюджет timeout; авторизация/CSRF; правила хранения и удаления. TTL 30 дней сейчас ограничивает срок дедупликации. Webhook, Owner/Trainer/Viewer и bootstrap из DOCX пока не реализованы.

## Исправления в текущем продолжении

Скачиваемый HTML теперь включает все варианты портрета Намига. Его встроенные страницы всегда demo, даже если hosted build live. Endpoint явно проверяет FORM_DELIVERY_ENABLED. Проверка: `node tests/build-review.mjs` (в конце возвращает сборку в demo).

## Проверки и команды

Node >=24, обычный ESM/vanilla JS, без React/Next. `npm run lint`, `npm test`, `npm run build`. Перед продолжением было 18 успешных unit-тестов. Браузерные тесты: `tests/form-browser.mjs`, `tests/portrait-browser.mjs`, `tests/release-a.mjs`, `tests/outcomes.mjs`; URL через TEST_BASE_URL. Они по умолчанию используют Chrome на Windows. Демонстрационные тесты не должны отправлять реальные заявки.

Локальный сервер ранее работал на http://127.0.0.1:4175/de/ . Проверить доступность, не убивать чужой процесс на 4173.

Vercel project: ak-loewen-release-a, scope zumeeeeer-6684s-projects, projectId prj_0kG9RBjUgIn4UktNF1qYU0cCgRvU. Root directory site, build npm run build, output dist. SSO preview отключён. Автодеплой Git выключен в vercel.json. Ранее CLI ошибочно выбрал production для первого deploy, поэтому preview создавали API /v13/deployments с target ОПУЩЕННЫМ, gitSource ref codex/release-a. Проверять ответ target=null и READY. Не печатать полную конфигурацию Vercel: там могут быть bypass secrets.

## Следующее действие

Сверить git status и этот файл. Продолжать независимые доработки Release A по DOCX, сохраняя отложенные пункты. После разрешения вернуться к Upstash — закончить backend, затем отдельно проверить полный путь браузер → API → Redis → Telegram. Перед каждым отчётом различать: код сохранён, локально проверен, развёрнут, реально доставляет.
