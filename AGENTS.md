# STOP — deprecated implementation branch

## Do not work in `codex/site-v1`

This branch is historical/recovery material only. It is **not** the current implementation branch.

Before any search or edit, leave this branch and read the live repository router:

**https://github.com/koss32/Ak-loewen/blob/Ak-loewen/index.md**

At the time of this marker, the current owner-approved implementation is `release-2/site/`, with approved code baseline `c8591e0aa1e197cdcc3eb850b174c6114467b595`.

Do not assume that pointer will remain forever: the `CURRENT HANDOFF` block in `Ak-loewen/index.md` is the canonical routing pointer for future agents.

Only inspect `codex/site-v1` when the owner explicitly requests historical comparison, rollback/recovery, or a v1-specific task. If such a historical task changes anything, do not promote this branch to current without explicit owner approval.